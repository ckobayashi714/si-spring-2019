
#! /usr/bin/env bash

COLUMNS=$(tput cols) 
title="VIEWING WORKSPACE" 
printf '\n%*s\n' "$COLUMNS" '' | tr ' ' =
printf "\n%*s\n\n" $(((${#title}+$COLUMNS)/2)) "$title"


git checkout master
git stash apply

