
#! /usr/bin/env bash

COLUMNS=$(tput cols) 
title="VIEWING SOLUTIONS" 
printf '\n%*s\n' "$COLUMNS" '' | tr ' ' =
printf "\n%*s\n\n" $(((${#title}+$COLUMNS)/2)) "$title"

git stash
git checkout solutions

